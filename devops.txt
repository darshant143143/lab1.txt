lab 1

git --version
git config --global user.name "Darshan2793"
git config --global user.email "darshant143143@gmail.com"
git config --list
mkdir lab1
cd lab1
git init
ls -a
touch index.txt
git status
echo "good morning">index.txt
cat index.txt
git add index.txt
git add .
git commit -m "initial commit"
git log
git remote add origin https://github.com/darshant143143/devoplab.git
git branch -M master
git push -u origin master
vi index.txt
cat index.txt
git add index.txt
git add .
git commit -m "changes made"
git log
git push -u origin master
git clone https://github.com/darshant143143/devoplab.git
git reset --hard


lab 2

mkdir git-demo-merge
cd git-demo-merge
git init
echo "version1">file.txt
git add .
git commit -m "initial commit"
git checkout -b feature
echo "feature version">file1.txt
git add .
git commit -m "changed in feature"
git checkout master
echo "main version">file1.txt
git add .
git commit -m "change in main"
git merge feature 
cat file1.txt
echo "allow main and feature branch">file1.txt
git add .
git commit -m "resolved merge conflict"
git log
            
----------

git --version
git config --global user.name "Darshan2793"
git config --global user.email "darshant143143@gmail.com"
git --list
mkdir demolab
cd demolab
git clone https://github.com/darshant143143/abcd.git
cd abcd
nano project.txt
git add .
git commit -m "initial commit"
git branch
git push -u origin main
git branch branch-a
git checkout branch-a
nano pro.txt
git add .
git commit -m "updated branch"
git log
git push origin branch-a























